# Project Status

## Completed
- Initialized Next.js app structure with App Router pages.
- Integrated Sanity (schemas, queries, client utilities).
- Set up Sanity project `1descy2t` with dataset `production`.
- Added `.env.local` with Sanity project and dataset.
- Added contact form API route and wired Resend email sending.
- Added category filters + fullscreen lightbox.
- Added masonry-style grid and minimalist layout direction.
- Updated fonts to Playfair Display (headings) + Manrope (body).
- Set background to light gray.
- Updated brand to "KABIUR RAHMAN RIYAD".
- Added `@sanity/vision` to fix Sanity dev error.
- Ignored `System Volume Information` in Next watch settings.

## To Do (You)
- Add `RESEND_API_KEY` to `.env.local` (do not paste in chat).
- Verify sender email `messages@riyad.pro.bd` in Resend.
- Start dev servers:
  - `pnpm dev`
  - `pnpm sanity dev`
- Create Sanity content:
  - Categories (Street, Travel, Documentary)
  - Site Settings (bio, portrait, email, contact blurb, toggle form)
  - Photos and Projects

## To Do (Me)
- Optional: send contact emails to both `messages@riyad.pro.bd` and `kabiur.riyad@gmail.com` if requested.
- Optional: refine project page layout or typography spacing if you want more changes.
